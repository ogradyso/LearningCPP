#include <limits>
#include <cstdio>
#include <cstdint>

//int main() {
//	int64_t b = std::numeric_limits<int64_t>::max();
//	int32_t c(b); //compiler complies because non-braced initialization allows for narrowing conversion
//	if (b != c) printf("Narrowing conversion!\n");
//}